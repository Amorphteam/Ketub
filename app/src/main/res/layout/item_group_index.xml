<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="item"
            type="com.amorphteam.ketub.model.TocGroupItem" />

        <variable
            name="indexClickListener"
            type="com.amorphteam.ketub.ui.main.tabs.toc.adapter.ClickListener" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/half_margin"
        android:gravity="center"
        android:orientation="horizontal">

        <LinearLayout
            android:id="@+id/view"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            >

            <TextView
                android:id="@+id/book_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="@dimen/xsmall_margin"
                android:text="@string/nosos_title"
                android:textAppearance="@style/TextAppearance.AppCompat.Body1"
                app:bookTitleGroup="@{item}"
                />

            <TextView
                android:id="@+id/book_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="@dimen/xsmall_margin"
                android:textSize="@dimen/description_text"
                android:text="@string/nosos_title"
                android:textColor="?attr/colorPrimaryVariant"
                android:textAppearance="@style/TextAppearance.AppCompat.Display1"
                app:bookNameGroup="@{item}"

                />
        </LinearLayout>

<!--        <ImageView-->
<!--            android:id="@+id/image"-->
<!--            android:layout_width="@dimen/small_image"-->
<!--            android:layout_height="@dimen/small_image"-->
<!--            android:layout_marginStart="@dimen/small_image"-->
<!--            android:src="@drawable/index_polygon" />-->

    </LinearLayout>
</layout>
