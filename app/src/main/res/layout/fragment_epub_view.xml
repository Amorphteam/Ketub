<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.amorphteam.ketub.ui.epub.fragments.EpubViewFragmentViewModel" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:id="@+id/main_epub_countiner"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@{viewModel.hideToolbar == true ? @color/background2 : @color/background1}"
            android:orientation="vertical"
            tools:context=".ui.epub.EpubViewer">

            <com.google.android.material.appbar.AppBarLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@android:color/transparent"
                app:elevation="0dp">

                <androidx.appcompat.widget.Toolbar
                    android:id="@+id/toolbar"
                    app:navigationIcon="@{viewModel.hideToolbar == true ? null : @drawable/ic_back}"
                    android:layout_width="match_parent"
                    android:layout_height="?actionBarSize" >

                    <TextView
                        android:id="@+id/book_name"
                        android:layout_width="match_parent"
                        android:text="@string/book_name"
                        android:layout_height="wrap_content"
                        style="@style/caption"
                        android:gravity="center"
                        />
                </androidx.appcompat.widget.Toolbar>
            </com.google.android.material.appbar.AppBarLayout>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <com.h6ah4i.android.widget.verticalseekbar.VerticalSeekBarWrapper
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:layout_gravity="end"
                    android:id="@+id/slider_countiner"
                    android:layout_marginBottom="?attr/actionBarSize"
                    android:translationZ="90dp">

                    <io.techery.progresshint.addition.widget.VerticalSeekBar
                        android:id="@+id/seekBar"
                        android:layout_width="0dp"
                        android:layout_height="0dp"
                        app:popupAlwaysShown="false"
                        app:popupDraggable="false"
                        style="@style/SeekBar"
                        app:popupLayout="@layout/seekbar_layout"
                        android:onProgressChanged="@{viewModel.updateVerticalSeekBar}"
                        app:popupStyle="follow"
                        app:seekBarRotation="CW90" />
                    <!-- Rotation: CW90 or CW270 -->
                </com.h6ah4i.android.widget.verticalseekbar.VerticalSeekBarWrapper>


                <androidx.cardview.widget.CardView
                    android:id="@+id/webView_countiner"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_marginBottom="?attr/actionBarSize"
                    app:cardCornerRadius="16dp"
                    app:cardElevation="@{viewModel.hideToolbar == true ? 0f : 4f}">

                    <WebView
                        android:id="@+id/webView"
                        app:loadUrl="@{viewModel.webViewUrl}"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent" />
                </androidx.cardview.widget.CardView>

                <EditText
                    style="@style/body"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="bottom|center"
                    android:inputType="number"
                    android:background="@android:color/transparent"
                    android:layout_marginBottom="@dimen/normal_margin"
                    android:gravity="center"
                    android:text="@{viewModel.pageNumber}"
                    android:paddingRight="@dimen/large_margin"
                    android:paddingLeft="@dimen/large_margin"
                    android:id="@+id/page_number"
                    android:textColor="@{viewModel.hideToolbar == true ? @color/primary2 : @color/onbackground2}"
                    />
            </FrameLayout>

        </LinearLayout>
        <View
            android:visibility="gone"
            android:id="@+id/bg"
            android:background="#99000000"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <include layout="@layout/sheet"
            app:viewModel="@{viewModel}"/>
    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>